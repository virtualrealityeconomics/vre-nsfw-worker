# Model = NudeNet v3 via the MIT-licensed `nudenet` package (per-body-part detector). The 320n.onnx
# weights ship INSIDE the wheel — no runtime/build download needed. nudenet pulls onnxruntime +
# opencv-python-headless (no ultralytics, no torch → zero Ultralytics code runs here).
nudenet>=3.4.2
pillow>=10
numpy>=1.24,<2
psycopg2-binary>=2.9
boto3>=1.34
requests>=2.31
